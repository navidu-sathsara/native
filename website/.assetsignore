# Never upload locally built installers. These directories are gitignored,
# but a manual `wrangler deploy` still sees the files on disk.
downloads/*
updates/*
!updates/latest.yml
!updates/latest-linux-arm64.yml
wrangler.jsonc
.assetsignore

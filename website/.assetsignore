# Never upload locally built installers that pile up in updates/ (the
# directory is gitignored except the two force-added update feeds, but a
# manual `wrangler deploy` from a dev machine sees the local files).
updates/*
!updates/latest.yml
!updates/latest-linux-arm64.yml
wrangler.jsonc
.assetsignore
